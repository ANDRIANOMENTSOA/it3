@using System.Data
@model DataSet

<table class="table table-bordered tablehead" id="newtb1">
    <thead>
        <tr style="background-color: RGB(173,255,47);">
            <th>CPN SECTOR</th>
            <th>FROM SECTOR</th>
            <th>TO SECTOR</th>
            <th>ARRIVAL</th>
            <th>DEPARTURE</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.ligne; i++)
        {
            <tr>
                @for (int j = 0; j < ViewBag.colone; j++)
                {
                    <td>@ViewBag.Liste[i, j]</td>
                }
            </tr>
        }
    </tbody>
</table>
